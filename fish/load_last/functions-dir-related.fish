

# idea is to use a single dir to store all config that relates to a function file in a functions dir that doesn't otherwise have a relevant config file
# get_shell_symbols
abbr ss get_shell_symbols
