## Hammerspoon Instructions

When change the Hammerspoon config, reload with:

```shell
hs -C -c "hs.reload()"
# -c command to run (lua code)
# -C enables print cloning from the Hammerspoon Console to this instance.
```
