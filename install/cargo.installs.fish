#!/usr/bin/env fish

cargo install --list
# --force to update

# JFryy/systemd-lsp
cargo install systemd-lsp --force

cargo install treeify --force

cargo install --git https://github.com/rydesun/fennel-language-server --force
