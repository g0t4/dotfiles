#!/usr/bin/env python3

import subprocess
import sys
from pathlib import Path


if len(sys.argv) < 2 or sys.argv[1] != "amend":
    subprocess.run([sys.executable, Path(__file__).with_name("core.py")], check=False)
