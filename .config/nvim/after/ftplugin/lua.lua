-- run in after so you guarantee that this is last to run (wins) vs bundled nvim ftplugin/lua.* scripts

-- print("my ftplugin/lua.lua") -- uncomment in other places, including nvim's bundled scripts to double check order (or use :scriptnames, IIRC those are in order)
