#!/bin/bash

# FYI this is no faster than using KM macro => shell script
# unselect text in devtools, watch how fast its selected aftger button press
#   button to this script is same speed to select text as the button to KM macro to run a similar script, almost instant
# delay is clearly visible after text is selected and before the first chunk arrives

/opt/homebrew/bin/hs -c 'AskOpenAIStreaming()'

