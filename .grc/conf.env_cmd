# grc color env command output
regexp=^([^=]*)=(.*)$
colours=unchanged,magenta,unchanged
=====
# multiline values: (subsequent lines after line with name), i.e. FZF_DEFAULT_OPTS
regexp=^\s+(.*)$
colors=unchanged,unchanged
# set second color to same as 3rd in prev rule